{
  "name": "auth-jwt-cookies-react",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check-format": "npx @biomejs/biome format './server' './client'",
    "format": "npx @biomejs/biome format --write './server' './client'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^1.9.2"
  }
}
